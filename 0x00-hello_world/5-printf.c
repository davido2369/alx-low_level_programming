#include <stdio.h>
/**
 * * main - A program that is using the print f to print a line
 * Return: 0 (success)
 */

int main(void)
{
	print("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
